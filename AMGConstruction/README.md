# Basic React Boiler Plate without using create-react-app. Nothing extra. Except bootstrap... that's in this package.

``npm install``
``npm start``

If getting webpack-dev-server related error:
npm install webpack-dev-server --save-dev